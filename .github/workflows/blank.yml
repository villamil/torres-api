name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Typescript compiler
      uses: iCrawl/action-tsc@v1
      with:
        # Optional name of the annotation author.
        job-name: compile
